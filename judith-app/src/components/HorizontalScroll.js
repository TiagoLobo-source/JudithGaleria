import React from 'react';
import './HorizontalScroll.css'; 
const CustomHorizontalScroll = ({ children }) => {
  return <div className="scroll-container">{children}</div>;
};

export default CustomHorizontalScroll;
